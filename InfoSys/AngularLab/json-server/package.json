{
  "name": "simple-server",
  "version": "1.0.0",
  "description": "Full fake REST API server",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "json-server": "^0.17.3"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {}
}