import { Under18Directive } from './under18.directive';

describe('Under18Directive', () => {
  it('should create an instance', () => {
    const directive = new Under18Directive();
    expect(directive).toBeTruthy();
  });
});
